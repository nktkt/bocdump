name: Release

env:
  ZIG_VERSION: 0.15.2

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  create-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Create release notes
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view ${{ github.ref_name }} --repo ${{ github.repository }} >/dev/null 2>&1 || \
            gh release create ${{ github.ref_name }} \
              --repo ${{ github.repository }} \
              --title "${{ github.ref_name }}" \
              --notes "Initial bocdump release."

  release:
    runs-on: ubuntu-latest
    needs: create-release
    permissions:
      contents: write
      id-token: write
      attestations: write
      artifact-metadata: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-linux
            artifact: bocdump-x86_64-linux
          - target: aarch64-linux
            artifact: bocdump-aarch64-linux
          - target: x86_64-macos
            artifact: bocdump-x86_64-macos
          - target: aarch64-macos
            artifact: bocdump-aarch64-macos

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Zig
        run: |
          set -euo pipefail
          zig_dir="zig-x86_64-linux-${ZIG_VERSION}"
          curl -fsSLo zig.tar.xz "https://ziglang.org/download/${ZIG_VERSION}/${zig_dir}.tar.xz"
          tar -xf zig.tar.xz
          echo "$PWD/${zig_dir}" >> "$GITHUB_PATH"
          "$PWD/${zig_dir}/zig" version

      - name: Build
        run: zig build -Doptimize=ReleaseSafe -Dtarget=${{ matrix.target }}

      - name: Package
        run: |
          mkdir -p dist/${{ matrix.artifact }}
          cp zig-out/bin/bocdump dist/${{ matrix.artifact }}/
          cp README.md LICENSE dist/${{ matrix.artifact }}/
          tar -C dist -czf ${{ matrix.artifact }}.tar.gz ${{ matrix.artifact }}

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload ${{ github.ref_name }} ${{ matrix.artifact }}.tar.gz --clobber

      - name: Attest release asset provenance
        uses: actions/attest@v4
        with:
          subject-path: ${{ matrix.artifact }}.tar.gz

  checksums:
    runs-on: ubuntu-latest
    needs: release
    permissions:
      contents: write
      id-token: write
      attestations: write
      artifact-metadata: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install SBOM dependencies
        run: npm ci

      - name: Download release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release download ${{ github.ref_name }} --repo ${{ github.repository }} --pattern "bocdump-*.tar.gz" --dir dist

      - name: Generate SHA256SUMS and SBOM
        run: |
          cd dist
          sha256sum bocdump-*.tar.gz > SHA256SUMS
          cd ..
          node scripts/generate-sbom.mjs > dist/bocdump.spdx.json

      - name: Attest release asset SBOM
        uses: actions/attest@v4
        with:
          subject-checksums: dist/SHA256SUMS
          sbom-path: dist/bocdump.spdx.json

      - name: Attest checksum provenance
        uses: actions/attest@v4
        with:
          subject-path: dist/SHA256SUMS

      - name: Upload SHA256SUMS and SBOM
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload ${{ github.ref_name }} dist/SHA256SUMS dist/bocdump.spdx.json --clobber

  smoke:
    runs-on: ubuntu-latest
    needs: checksums
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install smoke dependencies
        run: npm ci

      - name: Smoke test release asset
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: npm run smoke:release
